import 'dart:convert';
import 'dart:io';
import 'dart:math';

void main() {
  bool ulantalyby = true;

  while (ulantalyby) {
    List variant = ['tash, kagaz, kaichy'];
    final variants = stdin.readLineSync(encoding: utf8);
  }
}
